{
  "name": "creepjs",
  "version": "1.0.0",
  "description": "creepy device and browser fingerprinting",
  "main": "server.js",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "watch:dev": "pnpm exec rollup --config rollup.config.js -w & nodemon -d 3 ./server.js localhost 8000",
    "build:full": "BUILD_MODE=full NODE_OPTIONS=--no-warnings TSCONFIG_PATH=./tsconfig.build.json pnpm exec rollup --config rollup.config.js",
    "build:lite": "BUILD_MODE=lite NODE_OPTIONS=--no-warnings TSCONFIG_PATH=./tsconfig.build.json pnpm exec rollup --config rollup.config.js",
    "build:dev": "BUILD_MODE=all NODE_OPTIONS=--no-warnings TSCONFIG_PATH=./tsconfig.build.json pnpm exec rollup --config rollup.config.js",
    "autoprefixer": "postcss public/style.css -u autoprefixer -o public/style.prefix.css",
    "minify:css": "cleancss -o docs/style.min.css public/style.prefix.css",
    "build:css": "pnpm autoprefixer && pnpm minify:css",
    "build:js": "pnpm build:dev",
    "build": "pnpm -s build:js && pnpm -s build:css"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/abrahamjuliot/CreepJS.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/abrahamjuliot/CreepJS/issues"
  },
  "homepage": "https://github.com/abrahamjuliot/CreepJS#readme",
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.1.2",
    "@typescript-eslint/eslint-plugin": "^8.19.1",
    "@typescript-eslint/parser": "^8.19.1",
    "autoprefixer": "^10.4.20",
    "axios": "^1.7.9",
    "clean-css-cli": "^5.6.3",
    "eslint": "^10.0.0",
    "eslint-config-google": "^0.14.0",
    "eslint-config-prettier": "^10.0.0",
    "express": "^5.0.0",
    "nodemon": "^3.1.9",
    "postcss": "^8.4.49",
    "postcss-cli": "^11.0.0",
    "prettier": "3.8.3",
    "rollup": "^4.30.1",
    "terser": "^5.37.0",
    "typescript": "^7.0.0"
  }
}
